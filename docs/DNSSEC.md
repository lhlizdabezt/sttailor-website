# DNSSEC

DNSSEC is active for `sttailor.com` as of 2026-09-21. Cloudflare signs the zone; the DS record is published at the iNET registrar.

| Field | Value |
| --- | --- |
| Key tag | 2371 |
| Algorithm | 13 (ECDSAP256SHA256) |
| Digest type | 2 (SHA-256) |
| Digest | `19BEA7289C5D0BDC778C42B0771D8FECE5E3BB1E937AE05210F9AF98A7999065` |

Verification completed through both Cloudflare DNS-over-HTTPS and Google Public DNS: each returned the matching DS record with authenticated data (`AD=true`).

If DNSSEC must be rolled back, first disable DNSSEC at the registrar by removing the DS record, wait for the parent-zone TTL to expire, confirm public resolution remains healthy, then disable DNSSEC in Cloudflare. Never change DS values manually unless they are generated by Cloudflare for this zone; a mismatched DS can cause `SERVFAIL`.
